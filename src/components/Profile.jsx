const Profile = () => {
  const email = "usuario@ejemplo.com"; // temporal

  return (
    <div>
      <h2>Perfil del Usuario</h2>
      <p>Email: {email}</p>
      <button>Cerrar sesión</button>
    </div>
  );
};

export default Profile;
